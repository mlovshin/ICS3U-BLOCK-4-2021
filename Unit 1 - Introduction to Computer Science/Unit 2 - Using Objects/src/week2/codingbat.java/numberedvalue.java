package week2.codingbat.java;

public String firstHalf(String str) {
    String firsthalf = str.substring(0, str.length()/2); // to find the first half of a even word
    return firsthalf;
  }
    
}
